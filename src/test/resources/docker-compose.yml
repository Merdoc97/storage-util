version: '2'

services:
  minio:
    image: docker.io/bitnami/minio:2022
    ports:
      - '9006:9000'
      - '9007:9001'
    environment:
      MINIO_ROOT_USER: admin
      MINIO_ROOT_PASSWORD: password
